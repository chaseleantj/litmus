services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - app-data:/app/backend/data
    restart: unless-stopped

volumes:
  app-data:
